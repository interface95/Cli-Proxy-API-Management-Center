## 2026-03-27

### ⚙️ Antigravity credits_mode 前端支持 `16:00`

- [x] 为认证文件字段编辑器新增 `credits_mode` 选择器
  - 兼容读取 `credits_mode` / `credits-mode`，并从旧 `allow_overages` 推导 `off` / `fallback`
  - 保存时统一写入 `credits_mode`，移除旧字段，避免 UI 无法表达 `always`
- [x] 为 visual config 新增 `antigravity.credits-mode` 编辑支持
  - 读取优先使用 `credits-mode`，兼容旧 `allow-overages`
  - 保存时统一写入 `credits-mode`
- [x] 更新中英文俄文文案，避免界面继续暗示只有 `allow_overages` 可控
- [x] 完成前端验证

**修改的文件：**
- `src/features/authFiles/hooks/useAuthFilesPrefixProxyEditor.ts` — 增加 auth 文件 `credits_mode` 推导、规范化与保存逻辑
- `src/features/authFiles/components/AuthFilesPrefixProxyEditorModal.tsx` — 将旧布尔开关替换为 `credits_mode` 选择器
- `src/types/visualConfig.ts` — 新增 `AntigravityCreditsMode` 类型并调整 visual values
- `src/hooks/useVisualConfig.ts` — 增加 `antigravity.credits-mode` 读取与 YAML 回写兼容逻辑
- `src/components/config/VisualConfigEditor.tsx` — 将 Antigravity 布尔配置替换为模式下拉框
- `src/i18n/locales/en.json` — 新增 credits mode 文案
- `src/i18n/locales/zh-CN.json` — 新增 credits mode 文案
- `src/i18n/locales/ru.json` — 新增 credits mode 文案

**测试结果：**
- ✅ `lsp_diagnostics`（TS/TSX 改动文件）— 通过
- ✅ `pnpm type-check` — 通过
- ✅ `pnpm build` — 通过

**遇到的问题：**
- JSON locale 的 `lsp_diagnostics` 依赖 `biome`，当前环境未安装 → 使用 `pnpm type-check` 与 `pnpm build` 作为实际校验补充
- 旧 `allow_overages` 文件会在新 UI 中被规范化为 `credits_mode` → 加载阶段同步规范化基线，避免弹窗初始即显示脏状态
